import React from 'react'

interface Props {}

function StudentsList(props: Props) {
    const {} = props

    return (
        <div>
            <h1>Students List</h1>
        </div>
    )
}

export default StudentsList
