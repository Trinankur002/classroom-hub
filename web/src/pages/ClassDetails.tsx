import React from 'react'

interface Props { }

function ClassDetails(props: Props) {
    const { } = props

    return (
        <div>
            <h1>Class Details</h1>
        </div>
    )
}

export default ClassDetails
